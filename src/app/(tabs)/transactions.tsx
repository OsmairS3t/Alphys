import { View, Text } from 'react-native';
import { Container, Title } from '../styles/transactionStyle';

export default function Transactions() {
  return (
    <View>
      <Text>Listagem</Text>
    </View>
  )
}